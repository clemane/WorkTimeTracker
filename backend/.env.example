# Server
PORT=4001

# Seed users (format: "user1:password1,user2:password2")
SEED_USERS="admin:changeme"

# SMTP (email reports)
SMTP_HOST=localhost
SMTP_PORT=25
SMTP_SECURE=false
SMTP_TLS_REJECT_UNAUTHORIZED=true

# Email addresses
MAIL_FROM="Time Tracker <noreply@example.com>"
MAIL_TO=admin@example.com
